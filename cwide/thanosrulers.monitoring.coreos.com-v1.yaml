Version                                            Replicas                                           Ready                                              Reconciled                                         Available                                          Age                                                Paused                                             
.spec.version                                      .spec.replicas                                     .status.availableReplicas                          .status.conditions[?(@.type == Reconciled)].status .status.conditions[?(@.type == Available)].status  .metadata.creationTimestamp                        .status.paused                                     
