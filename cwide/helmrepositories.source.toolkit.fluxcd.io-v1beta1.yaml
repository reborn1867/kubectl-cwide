URL                                            Ready                                          Status                                         Age                                            
.spec.url                                      .status.conditions[?(@.type=="Ready")].status  .status.conditions[?(@.type=="Ready")].message .metadata.creationTimestamp                    
