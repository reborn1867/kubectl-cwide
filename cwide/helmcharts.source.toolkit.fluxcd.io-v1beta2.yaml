Chart                                          Version                                        Source Kind                                    Source Name                                    Age                                            Ready                                          Status                                         
.spec.chart                                    .spec.version                                  .spec.sourceRef.kind                           .spec.sourceRef.name                           .metadata.creationTimestamp                    .status.conditions[?(@.type=="Ready")].status  .status.conditions[?(@.type=="Ready")].message 
